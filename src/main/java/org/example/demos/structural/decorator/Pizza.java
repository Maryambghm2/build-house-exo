package org.example.demos.structural.decorator;

// Structural decoratort permet d'ajouter des modifications à un objet deja existant.

// Interface qui permet de représenter une Pizza
public interface Pizza {
    String getDescription();
    double getCost();
}
